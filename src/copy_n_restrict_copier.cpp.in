#include <algorithm>

#include "${HEADER_FILE_NAME}"

// clang-format off
void ${COPIER_FUNCTION_NAME}(
    ${DATA_TYPE}* __restrict dest, ${DATA_TYPE} const* __restrict src,
    std::size_t n) {
  std::copy_n(src, n, dest);
}
// clang-format on
